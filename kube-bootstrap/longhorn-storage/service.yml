apiVersion: v1
kind: Service
metadata:
  name: longhorn
  namespace: longhorn-system
spec:
  type: NodePort
  ports:
    - name: https-in
      port: 8000
      targetPort: 8000
      nodePort: 30003
  selector:
    app: longhorn-ui